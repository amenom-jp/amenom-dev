CREATE TABLE "artists" (
    "artist_id" TEXT NOT NULL,  -- id
    "name" TEXT NOT NULL        -- アーティストの名前
);

CREATE TABLE "label" (
    "label_id" INTEGER NOT NULL,  -- id
    "name" TEXT NOT NULL          -- レーベルの名前
);